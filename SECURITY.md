# Security policy

Report a vulnerability privately to the maintainers rather than opening a public issue. Do not include API keys, screenshots containing sensitive information, or other secrets in a report.

This baseline has no account service, server, analytics, or persistent user data. Future credential handling must remain session-only and must never expose keys to page scripts, URLs, logs, or telemetry.
